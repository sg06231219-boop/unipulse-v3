name: Trigger Render
on:
  workflow_dispatch:
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -s -X POST "https://api.render.com/v1/services/srv-d8j9j88jo6nc73e449ig/deploys" \
            -H "Authorization: Bearer rnd_YRp3lawJR0ofKVFmbNqcSYuPvu5N" \
            -H "Content-Type: application/json" \
            -d '{"clearCache": true}' | python3 -c "
          import sys,json
          d=json.load(sys.stdin)
          print(f'deploy_id={d.get(\"id\",\"?\")} status={d.get(\"status\",\"?\")}')
          "